{
    "name": "hello_world",
    "version": "2.0.0",
    "description": "Secure Hello World server with Express, Helmet, Rate Limiting, CORS, and HTTPS support",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "start:https": "ENABLE_HTTPS=true node server.js",
        "generate-certs": "mkdir -p certs && openssl req -x509 -newkey rsa:4096 -keyout certs/key.pem -out certs/cert.pem -days 365 -nodes -subj '/CN=localhost'",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "express": "^4.21.2",
        "helmet": "^8.1.0",
        "express-rate-limit": "^7.4.1",
        "cors": "^2.8.5"
    },
    "engines": {
        "node": ">=14.0.0"
    },
    "author": "hxu",
    "license": "MIT"
}